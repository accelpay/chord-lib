# Chord Lib

#### Install

```
npm install @accelpay/chord-lib
```

#### Quickstart

```
const {
    unavailableVariantIds,
    setZip,
    setLineItems
} = useAccelpay({ lineItems: [{ variantId: 123, quantity: 2 }] });
```
